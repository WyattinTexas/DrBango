# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
world-2d-backup/
world-unity/

# 0094 security patch (7/30): macOS folder metadata enumerates the site layout
# on a public Pages repo, and belt-and-braces on env files.
.DS_Store
**/.env

# scraped HTML cache for beta3/tools/build-words.py (13MB, regenerable)
beta3/tools/.wordcache/
